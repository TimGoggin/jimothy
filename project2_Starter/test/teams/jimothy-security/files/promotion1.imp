
function main() {
    var y = false;
    var z = true;
    var x = get_bool_s();

    if (x) then {
        y = true;
    }

    if (!y) then {
        z = false;
    }

    print_bool(z);
    return;
}

/* On input false, this should result in a SecurityError, but the code will run without errors. */
/* This is because of the path not taken situation. */