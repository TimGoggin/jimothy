function printHi() {
    print_str("hi");
    return;
}

function main() {
    var x = get_int_s();

    if (x == 5) then {
        printHi();
    }

    printHi();

    return;
}

/* On input 3, this should result in a SecurityError, but the code will run without errors. */
/* This is because of the path not taken situation. */