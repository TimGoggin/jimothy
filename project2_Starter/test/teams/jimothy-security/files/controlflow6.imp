
function main() {
    var x = get_int_s();

    var y = 0;

    while (y < 5) do {
        if (y == x) then {
            print_int(x);
            return;
        }

        y = y + 1;
    }

    print_int(y);
    return;
}

/* On input 8, this should result in a SecurityError, but the code will run without errors. */
/* This is because of the path not taken situation. */