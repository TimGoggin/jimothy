
function main() {
    var s = get_str_s();
    var x = 0;

    if (s == "hi") then {
        x = 5;
    } else {
        s = "bye";
    }

    print_int(x);

    return;
}

/* On input "iloveoCaml", this should result in a SecurityError, but the code will run without errors. */
/* This is because of the path not taken situation. */