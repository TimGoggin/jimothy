
function main() {
    var x = get_int_s();

    var y = 0;

    while (y < 5) do {
        if (y == x) then {
            print(y);
            return;
        }

        y = y + 1;
    }

    return;
}
