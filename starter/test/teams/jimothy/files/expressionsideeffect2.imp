function main() {
  var a ;
  var b ;
  var c ;
  
  a = prompt_int("enter: ") ;
  b = prompt_bool("enter: ") ;
  c = prompt_int("enter: ") ;

  function mult(var x) {
    a = a * x;
    return a;
  }

  if (b = (mult(2) == c))  then {
    if (b) then {
      print_bool(b);
      return;
    }

    print_int(100000000);
    return ;
  }

  print_bool(mult(2) == c) ;
  return ;
}
